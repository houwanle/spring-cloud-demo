server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/houwanle/spring-cloud-config.git  #gittee上的仓库名称
          username: houwanle
          password: lele1995@gitee
          # 搜索目录
          search-paths:
            - spring-cloud-config
      #读取分支
      label: master

#服务注册到eureka地址
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka